<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Main Settings Menu -->
    <LinearLayout
        android:id="@+id/layoutMainSettings"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="20dp"
        android:gravity="center_horizontal">

        <Button
            android:id="@+id/btnMenuPushUssd"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:text="Push &amp; USSD Messaging"
            android:textSize="16sp"
            android:layout_marginBottom="15dp"
            android:backgroundTint="#2196F3"/>

        <Button
            android:id="@+id/btnMenuSmsWebhook"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:text="SMS Webhook"
            android:textSize="16sp"
            android:layout_marginBottom="15dp"
            android:backgroundTint="#4CAF50"/>

        <Button
            android:id="@+id/btnMenuUnifiedLog"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:text="Unified Log"
            android:textSize="16sp"
            android:layout_marginBottom="15dp"
            android:backgroundTint="#FF9800"/>

        <Button
            android:id="@+id/btnMenuSystem"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:text="System Settings"
            android:textSize="16sp"
            android:layout_marginBottom="15dp"
            android:backgroundTint="#607D8B"/>
    </LinearLayout>

    <!-- Sub Menu Container -->
    <LinearLayout
        android:id="@+id/layoutSubMenu"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="#FFFFFF"
        android:visibility="gone"
        android:clickable="true"
        android:focusable="true">

        <!-- Header with Back Button -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="10dp"
            android:background="#EEEEEE"
            android:gravity="center_vertical">
            
            <TextView
                android:id="@+id/tvSubMenuTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Settings"
                android:textSize="18sp"
                android:textStyle="bold"
                android:paddingStart="10dp"/>

            <ImageButton
                android:id="@+id/btnCloseSubMenu"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:src="@android:drawable/ic_menu_close_clear_cancel"
                android:background="?attr/selectableItemBackground"/>
        </LinearLayout>

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="15dp">
            
            <LinearLayout
                android:id="@+id/containerSubMenuContent"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">
                <!-- Content will be added dynamically or visible/gone -->
            </LinearLayout>
        </ScrollView>

    </LinearLayout>

</FrameLayout>
